# 專案特定規則 (Project Rules)

1. **中文計畫書 (Chinese Plans Only)**: 所有提供的 `implementation_plan.md` 與相關規劃文件，必須使用 **繁體中文** 撰寫。
2. **自動 Git Commit (Automatic Git Commits)**: 只要有任何程式碼變動（新增、修改、刪除、還原），都必須執行 `git commit`。
3. **Commit 訊息規範**: 必須使用 **使用者下達的原始指令文字**，或是**執行的PLAN名稱**(優先) 作為 `git commit -m` 的參數。
