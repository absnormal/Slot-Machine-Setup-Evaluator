# 老虎機線獎辨識工具 — 使用說明

## 這是什麼工具

用來驗證實機老虎機「賠率設定是否正確」的工具。流程是：

> **建立遊戲模板 → 辨識／填入盤面 → 自動結算贏分**

可以自己手動填盤面，也可以上傳實機截圖讓系統自動辨識結算。

---

## 開始前的準備

1. **開啟工具**：用瀏覽器打開工具網址。

2. **設定 Gemini 金鑰（選用）**：點右上角 ⚙️ 齒輪，填入 **Gemini API 金鑰**（只存在你自己的瀏覽器，不會上傳）。
   - 沒填也能用：手動填盤面、**快速辨識（免金鑰）**、結算都正常。
   - 有填才能用：賠率表自動辨識、截圖的 Gemini 辨識。

3. **介面模式（右上角切換鈕）**：工具分兩種畫面，點右上角的「簡易模式／完整模式」鈕切換。

   | 模式 | 畫面內容 | 適合 |
   |---|---|---|
   | **簡易模式**（預設）| 只有「手動驗算」「截圖辨識」，最上方一條**模板快速載入列** | 已經有模板，只想驗算的人 |
   | **完整模式** | 多出「**建立模板**」區（可從頭建立或編輯遊戲規則）| 要新增／修改一款遊戲 |

   > 簡單說：**要建立或改模板就切到完整模式；只是要驗算就用簡易模式。**

---

## 三個步驟一覽

| 步驟 | 名稱 | 做什麼 | 何時用 |
|---|---|---|---|
| 1 | 建立模板 | 設定遊戲規則（盤面、線獎、賠率表）| 第一次建一款遊戲 |
| 2 | 手動驗算 | 自己填盤面，即時算贏分 | 驗證單一盤面 |
| 3 | 截圖辨識 | 上傳實機截圖，自動辨識盤面 | 用實機畫面驗算 |

> 各區以「手風琴」呈現，一次展開一個；點標題列即可切換。

---

## 步驟 1：建立遊戲模板（完整模式）

把一款遊戲的規則「教」給工具，之後所有辨識與結算都靠這份模板。

1. 填**平台名稱**、**遊戲名稱**。
2. 設定**盤面大小**（幾列 × 幾行）。
3. 選**線獎模式**：固定線獎（Paylines）／全路線（All Ways）。
4. **設定線獎連線**：上傳連線圖框選提取，或直接打字輸入。
5. **設定賠率表**：上傳賠率表截圖讓系統辨識（需 Gemini 金鑰），或手動輸入（格式：`符號名 賠率1 賠率2 …`）。
6. **回答特殊機制問卷**（有才勾）：乘倍列、單線乘倍、動態乘倍、收集金幣／JP、雙重／三重符號、雙向連線等。
7. 按**建立模板**完成。

---

## 步驟 2：手動驗算

自己擺一個盤面，立刻看到工具怎麼算贏分——用來確認規則對不對。

1. 填**押注金額**。
2. **填盤面**：
   - 畫筆模式：從符號清單選一個，點／拖到格子上。
   - 鍵盤模式：每格直接打符號名稱（可貼上整批）。
3. 右側即時顯示**結算結果**（中獎線、SCATTER、收集金幣等明細）。
4. 按 **Enter** 可把這局贏分加進「總資產」累計。

---

## 步驟 3：截圖辨識

用實機截圖驗算，不用自己一格一格填。

1. **上傳截圖**（可一次多張，縮圖列切換）。
   - 小技巧：用系統截圖（如 Windows `Win+Shift+S`）框好畫面後，直接在這個區塊按 `Ctrl+V` 貼上，會立即自動辨識結算。
2. **框選辨識區域**：用滑鼠框出盤面範圍（必要時也框乘倍列、押注區）。
3. 選一種辨識方式：
   - **快速辨識（免金鑰）**：系統內建、在瀏覽器內用模板符號圖比對，**不用金鑰、零延遲**。
   - **Gemini 辨識**：呼叫雲端 AI（需先設定金鑰），辨識較強但有網路延遲。
4. 自動顯示辨識出的盤面 + 結算結果。
5. 可按 **↑** 把這個盤面送到「手動驗算」微調。

---

## 其他實用功能

- **總資產追蹤**：在手動驗算／截圖辨識按 Enter 累計贏分，數字會記住。
- **快捷鍵**：
  - 方向鍵 `↑ ↓`：切換區塊（並在驗算／辨識之間互傳盤面）。
  - `Enter`：建立模板區＝建立模板；驗算／辨識區＝把贏分加進總資產。
- **深色模式**：設定面板可切換。

---

## 常見問題

| 狀況 | 怎麼辦 |
|---|---|
| 符號辨識不到／認錯 | 確認模板賠率表有這個符號；快速辨識需要模板有符號縮圖 |
| Gemini 辨識無法使用 | 到 ⚙️ 確認已填金鑰，或改用「快速辨識（免金鑰）」 |
| 結算數字怪怪的 | 回去檢查模板的賠率表與特殊機制設定是否正確 |
| 截圖辨識結果偏掉 | 重新框選盤面範圍，框準一點 |
